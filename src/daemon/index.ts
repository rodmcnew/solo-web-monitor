import { BindingKey } from '@loopback/core';
import { SoloWebMonitorApplication } from '../application';
import { DemoDataServiceService } from '../services';

export async function setDatabaseToDemoData(app: SoloWebMonitorApplication) {
//@TODO move somewhere else, this isn't really a deamon?
}
